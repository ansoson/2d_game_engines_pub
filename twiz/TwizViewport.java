package twiz;

import engine.Viewport;
import javafx.scene.paint.Color;

public class TwizViewport extends Viewport {
    public TwizViewport(Double x, Double y, Double width, Double length, Color color) {
        super(x, y, width, length, color);
    }
}
