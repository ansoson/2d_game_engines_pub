package engine.UITools;

import javafx.scene.paint.Color;

public class BasicRoundedBox extends UIElement {
    public BasicRoundedBox(Double x, Double y, Double width, Double length, Color color) {
        super(x, y, width, length, color);
    }
}
